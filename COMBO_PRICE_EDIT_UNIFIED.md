# 套餐价格编辑统一优化

## 问题描述

在编辑套餐弹窗中，价格相关的设置分散在多个区域，导致用户体验不够直观：

### 问题1：套餐价格和折扣分离
- **套餐价格区域**：设置基础价格（basePrice）
- **折扣区域**：单独设置折扣（discount）
- 两个区域分开，用户需要在不同位置设置价格相关信息

### 问题2：商品额外费用编辑位置不一致
- **固定套餐模式**：在 `ComboItemsInput` 组件的商品列表中编辑额外费用
- **可选套餐模式**：在 `ComboItemGroupsConfig` 组件的模态框中编辑额外费用
- 两种模式的编辑位置不一致，用户体验混乱

## 解决方案

### 优化1：统一套餐定价区域

将"套餐价格"和"折扣"合并到一个统一的"套餐定价"区域：

**固定套餐模式**：
```
套餐定价
├─ 商品总价（自动计算）：显示所有商品价格总和
├─ 折扣设置（可选）：
│  ├─ 折扣类型：固定金额 / 百分比
│  └─ 折扣金额：输入具体折扣值
└─ 最终售价：自动计算并高亮显示
```

**可选套餐模式**：
```
套餐定价
└─ 套餐价格：手动输入基础价格（顾客选择分组商品后，各选项的额外费用将在此价格基础上累加）
```

注意：可选套餐不支持折扣功能，因为价格计算逻辑更复杂（基础价格 + 各分组选项的额外费用），折扣应用场景不明确。

### 优化2：统一商品额外费用编辑位置

**固定套餐模式（保持不变）**：
- 在商品列表中直接编辑额外费用
- 商品列表是主要的配置界面
- 用户可以直观地看到所有商品及其价格
- 价格会自动汇总到套餐总价

**可选套餐模式（优化）**：

修改前：
- 在"添加商品"模态框中编辑额外费用
- 用户需要在选择商品的同时设置价格
- 不够直观，容易遗漏

修改后：
- 移除模态框中的额外费用编辑功能
- 在分组的商品Tag列表中直接编辑额外费用
- 点击价格标签（如 `+¥0.00`）即可进入编辑模式
- 支持回车确认或点击"确定"按钮保存

## 技术实现

### 修改的文件

`src/pages/MenuCenter/components/ComboItemGroupsConfig.tsx`

### 主要变更

1. **添加状态管理**
```typescript
const [editingItemId, setEditingItemId] = useState<string | null>(null);
const [editingPrice, setEditingPrice] = useState<number>(0);
```

2. **移除模态框中的额外费用输入框**
- 将原来的 `Col span={9}` 额外费用输入框移除
- 简化为只显示商品原价

3. **在Tag列表中添加内联编辑功能**
- 将原来的 `Tag` 组件改为自定义的卡片样式
- 点击价格标签进入编辑模式
- 显示 `InputNumber` 和"确定"、"取消"按钮
- 支持回车键快速确认

4. **更新保存逻辑**
- 模态框只负责添加/移除商品到分组
- 额外费用的编辑在主界面完成
- 保留原有商品的额外费用设置

## 用户体验改进

### 价格设置区域优化
1. **统一的定价区域**：所有价格相关设置集中在"套餐定价"区域
2. **清晰的层次结构**：
   - 固定套餐：商品总价 → 折扣设置 → 最终售价
   - 可选套餐：套餐价格（简洁明了，无折扣干扰）
3. **视觉高亮**：最终售价用绿色大字号显示，一目了然

### 商品额外费用编辑优化
1. **更直观**：价格编辑在主界面进行，不需要打开模态框
2. **更高效**：可以快速调整多个商品的价格，无需重复打开模态框
3. **更一致**：与固定套餐模式的编辑方式保持一致的逻辑
4. **视觉反馈**：
   - 有额外费用的商品显示绿色（`#52c41a`）
   - 无额外费用的商品显示灰色（`#999`）
   - 鼠标悬停时显示提示"点击编辑额外费用"

## 测试要点

### 创建套餐
1. 可以自由选择套餐类型（固定套餐/可选套餐）
2. 类型选择后会显示对应的配置界面
3. 切换类型时会清空已配置的数据（有提示）

### 编辑套餐
1. **套餐类型选择器被禁用**，不能修改类型
2. 显示警告提示："⚠️ 套餐创建后不能修改类型"
3. 其他配置项可以正常编辑

### 固定套餐模式
1. 商品总价自动计算并显示
2. 可以设置折扣（固定金额或百分比）
3. 最终售价自动计算：商品总价 - 折扣
4. 商品列表中可以编辑每个商品的额外费用
5. 额外费用变化时，商品总价自动更新

### 可选套餐模式
1. 只显示套餐价格输入框，无折扣设置
2. 添加商品到分组后，价格默认为 `+¥0.00`
3. 点击价格标签可以进入编辑模式
4. 输入价格后按回车或点击"确定"保存
5. 点击"取消"放弃修改
6. 删除商品时保留其额外费用设置（以便重新添加时恢复）
7. 切换分组时，商品的额外费用不受影响

### 多选分组的选择数量
1. 多选分组显示"N选 [输入框]"格式
2. 用户只需输入"选几个"，系统自动设置为必选该数量
3. 显示提示："(顾客必选M项)"
4. 选择数量不能超过分组内的商品数量
5. 添加商品后，如果选择数量超出，自动调整
6. 删除商品后，如果选择数量超出，自动调整
7. minSelections 和 maxSelections 始终相等（必选固定数量）

## 业务规则

### 1. 套餐类型不可变更
- 套餐创建后，类型（固定套餐/可选套餐）不能修改
- 编辑模式下，套餐类型选择器会被禁用
- 显示警告提示："⚠️ 套餐创建后不能修改类型"

**原因**：
1. 固定套餐和可选套餐的数据结构不同（itemGroups、价格计算逻辑等）
2. 类型切换会导致已有配置丢失，可能引起数据不一致
3. 避免用户误操作导致套餐配置错乱

**建议**：
- 如需更改套餐类型，请创建新套餐并删除旧套餐
- 在创建套餐时仔细选择类型

### 2. 多选分组的"N选M"规则（符合餐饮行业标准）
- **"N选M"表示：从N个商品中必须选择M个**
- 例如："5选2" = 从5个商品中必须选2个
- minSelections 和 maxSelections 始终相等，确保顾客必须选择固定数量

**业务逻辑**：
- 用户只需输入"选几个"（分子），系统自动设置 minSelections = maxSelections = 输入值
- 商品数量（分母）由系统自动计算，等于分组内的商品总数
- 选择数量不能超过商品数量

**自动调整规则**：
1. 添加商品后，如果选择数量 > 商品总数，自动调整为商品总数
2. 删除商品后，如果选择数量 > 剩余商品数，自动调整为剩余商品数
3. minSelections 和 maxSelections 始终保持一致

**用户界面**：
- 显示格式："N选 [输入框] (顾客必选M项)"
- 例如："5选 2 (顾客必选2项)"
- 未添加商品时，显示："请先添加商品"
- 输入框的 max 属性限制为商品数量

**显示示例**：
- 5个商品，选2个：`5选 [2] (顾客必选2项)`
- 3个商品，选1个：`3选 [1] (顾客必选1项)`
- 4个商品，选4个：`4选 [4] (顾客必选4项)` - 全选

## 兼容性

- 保持与现有API的完全兼容
- 不影响固定套餐模式的功能
- 数据结构保持不变（`CreateComboItemPayload` 中的 `additionalPrice` 字段）

## 套餐列表显示优化

### 增强的套餐信息展示（两处）

#### 1. 分类下的套餐列表（List 样式）
位置：选择分类后显示的套餐列表

**套餐类型标识**：
- 固定套餐：显示 `固定套餐` 标签（青色）
- 可选套餐：显示 `可选套餐` 标签（紫色）

**固定套餐显示**：
- 显示所有包含的商品和数量
- 例如：`汉堡 ×1` `薯条 ×1` `可乐 ×1`

**可选套餐显示**：
- 显示所有分组信息
- 每个分组显示：
  - 分组名称和选择规则（如：`主食 (5选1)`、`饮品 (3选2)`）
  - 分组内的所有商品
  - 商品的额外费用（如有）：`可乐 +¥2.00`

**示例**：

固定套餐：
```
[套餐] [固定套餐] 经典套餐
包含商品: [汉堡 ×1] [薯条 ×1] [可乐 ×1]
原价: $15.00
```

可选套餐：
```
[套餐] [可选套餐] 自选套餐
套餐分组:
  [主食 (5选1)] [汉堡] [鸡肉卷] [鸡腿] [鸡翅 +¥2.00] [薯条]
  [饮品 (3选1)] [可乐] [雪碧] [橙汁 +¥1.00]
原价: $12.00
```

#### 2. 套餐管理表格（Table 样式）
位置：套餐管理 Tab 页的表格

**套餐名称列**：
- 显示套餐名称
- 下方显示类型标签：`固定套餐`（青色）或 `可选套餐`（紫色）

**包含商品列**：
- **固定套餐**：显示所有商品和数量（如：`汉堡 ×1` `薯条 ×1`）
- **可选套餐**：按分组显示
  - 每个分组一行
  - 显示分组名称和选择规则：`主食 (5选1)`
  - 显示前3个商品，超过3个显示"等N项"
  - 商品带额外费用标识：`可乐 +¥2.00`

**表格示例**：

| 套餐名称 | 包含商品 | 分类 | 原价 | 折扣 | 售价 |
|---------|---------|------|------|------|------|
| 经典套餐<br/>`固定套餐` | `汉堡 ×1` `薯条 ×1` `可乐 ×1` | 套餐 | $15.00 | 无 | $15.00 |
| 自选套餐<br/>`可选套餐` | `主食 (5选1)` `汉堡` `鸡肉卷` `鸡腿` 等5项<br/>`饮品 (3选1)` `可乐` `雪碧` `橙汁 +¥1.00` | 套餐 | $12.00 | 无 | $12.00 |

## 日期

2026-03-10
